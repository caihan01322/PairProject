spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pairwork?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    thymeleaf:
      cache: false
      encoding: utf-8
      prefix: classpath:/templates/
      suffix: .html

server:
  port: 8080
  servlet:
    context-path: /pairwork

